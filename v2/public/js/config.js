/**
 * ACL GESTION v2 - Configuration
 * For app.acl-gestion.com (Node.js API)
 */
const CONFIG = {
    // API URL - same domain for app.acl-gestion.com
    API_URL: '/api',

    // Landing page URL
    LANDING_URL: 'https://acl-gestion.com',

    // Auth keys
    TOKEN_KEY: 'acl_token',
    USER_KEY: 'acl_user',

    // App settings
    APP_NAME: 'ACL GESTION',
    VERSION: '2.0.0'
};
